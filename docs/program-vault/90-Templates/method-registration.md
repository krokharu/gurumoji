---
note_id: template-method-registration
note_type: template
title: 分析手法の登録票
status: proposed
updated: 2026-09-20
tags:
  - gurumoji/template
---

# 分析手法の登録票

手法の定義をプログラム資料側で版管理し、研究用Vaultには利用した定義の版を保存する。外部ツールによる分析と手作業の分析にも同じ票を使う。

| 項目 | 記入ルール |
| --- | --- |
| `method_id` | 英小文字・数字・アンダースコア。名称変更でも維持 |
| `method_version` | 計算法・前処理・出力契約の変更で更新 |
| 表示名・目的 | 解こうとする問いと結果の読み方 |
| `execution_kind` | `builtin`／`external`／`manual` |
| 実装状態 | `planned`／`available`／`deprecated`。登録できることと計算できることを区別 |
| 入力単位・必須データ | 会話／話者／発話／形態素等、必須列・ID・入力schema |
| 対象・除外 | 条件、対象集合の作成法、欠測・1話者・空データの扱い |
| パラメーター | 型・既定値・範囲・計算への影響 |
| 計算・実施手順 | 式または質的分析の工程、解析器・モデル・環境・seed |
| 結果schema | 必須成果物、JSON schema、CSV列・単位・行キー、図の形式 |
| UI入口・設定 | 分析画面内または既存画面への入口、対象・パラメーターの入力部品、前提・実行可否・実行先、試行／本実行。読取専用・手動・外部実行の場合はその操作 |
| UI結果・根拠 | 入出力schema版と対応する表示部品・UI版、主表示、根拠の解決・条件・履歴・旧run表示。未知の型を利用可能と扱わない |
| UI比較・再利用 | 比較可能な条件、選択行／全件、次の手法への接続、出力。対象外の操作は理由と代替の入口 |
| UI状態と検証 | 空・未判定・部分失敗・stale・利用不可、未保存変更・取消の扱い。設定から固定結果・根拠・再表示までの架空データによる操作検証 |
| 根拠規則 | 必須の発話参照／対象集合／表の行キー、支持・反証の区別 |
| 更新判定 | fingerprintに含める入力・条件・環境 |
| 保存分類 | DB台帳、固定CSV／JSON、Markdownの役割 |
| 限界 | 言えること、言えないこと、確認を要する条件 |
| 出典 | 手法の一次資料、実装元、確認日 |
| 検証 | 架空データ、期待結果、失敗条件、確認者 |

## 手法を登録する際の確認

- [ ] 同じ入力・条件・手法版を識別できる。
- [ ] 結果の行と入力を照合できる。
- [ ] 空・欠測・実行不可・失敗を区別する。
- [ ] 全件データを保存し、画面のプレビューから結果を作らない。
- [ ] 見解には出所と根拠がある。
- [ ] 既存のコード・メモ・別手法の結果を上書きしない。
- [ ] 原文・実行条件・結果ファイルへのリンクを確認した。
- [ ] 設定・実行（または読取／手動登録／取込）と結果・根拠のUIが同じ手法版・schemaに対応する。
- [ ] 比較・追加分析・出力の可否と理由を画面で確認できる。
- [ ] 計算・保存/API・設定UI・結果UI・再利用UI・操作検証の状態を分け、モックだけで利用可能としていない。

UIの詳細と網羅確認は[[40-Design/core-handler-routing-reorganization-plan]]の「分析手法とUIの対応確認」。これらの欄は追加設計で、現行レジストリへの自動検証の組込みは未実装。

正式な登録手順：[[40-Design/method-rules]]。結果ノート：[[research-result]]。
